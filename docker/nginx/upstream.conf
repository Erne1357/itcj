# Archivo generado automaticamente por deploy.sh
# NO EDITAR MANUALMENTE - se sobrescribe en cada deploy
# Backend activo: blue (valor inicial)

upstream backend {
    ip_hash;
    server backend-blue:8000 max_fails=3 fail_timeout=30s;
    keepalive 32;
}
