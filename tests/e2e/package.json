{
  "name": "itcj-helpdesk-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E suite for the ITCJ Helpdesk app (HTMX boosted navigation).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
